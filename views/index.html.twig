{% extends '/layouts/base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
<script type="module" src="js/user.js"></script>

<div class="card">
    <div class="card-header">
        Assistant GODS - Selected Character : 
        <select id="favoriteCharacter" class="select">
{% for character in characters %}
        <option value="{{ character.id_Character }}">{{ character.nom }}</h2>
{% endfor %}
</select>
    </div>
    
    <div class="card-body chat-body">
    </div>
    <div id="error" class="error"></div>
    <div class="card-footer">
        <div id="prompt-form">
            <div class="row align-items-center">
                <div class="d-flex justify-content-between textarea-chat">
                    <textarea id="prompt-field" class="form-control" style="flex:1;" placeholder="Écrire un message"></textarea>
                    <button id="send" class="send-button"></button>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}